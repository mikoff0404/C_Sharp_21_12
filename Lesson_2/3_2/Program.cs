
int[] array = { -10, 5, -3, -5, 1, 6, -7, 4, 9, -2 };

// Инвертировать каждый элемент массива
for (int i = 0; i < array.Length; i++)
{
    array[i] = -1 * array[i];
    Console.Write(array[i] + " ");

}





